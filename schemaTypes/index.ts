import {project} from './project'
import {tag} from './tag'
import {imageAlt} from '../customTypes'

export const schemaTypes = [
  project,
  tag,
  //Underneath are custom created types
  imageAlt,
]
